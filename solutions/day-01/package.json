{
  "name": "day-01",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist ; cp -r assets dist ; esbuild $(aaargh config.build.json)",
    "start": "node dist/part-01.js | tail -5 ; node dist/part-02.js | tail -5"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "aaargh": "^1.0.3",
    "esbuild": "^0.14.2",
    "readline": "^1.3.0"
  }
}
